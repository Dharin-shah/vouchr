name: Release

# Manual-only placeholder so this file is a VALID workflow (never auto-runs, never red-X's the
# repo). Signed releases / SLSA provenance are out of scope until there are publish credentials
# and signing keys. When that lands, switch the trigger to `on: push: tags: ['v*']`, add
# `permissions: { contents: write, id-token: write }`, and the build/sign/publish steps.
on:
  workflow_dispatch:

jobs:
  not-configured:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Signed releases are not configured yet."
          echo "See CHANGELOG.md (Deferred) and DEPLOYMENT.md; wire publish creds + signing keys to enable."
