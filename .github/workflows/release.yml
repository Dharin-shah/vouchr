name: Release

# Manual-only until publish credentials and signing keys exist. When that lands,
# switch the trigger to version tags, add contents/id-token permissions, and wire
# the build/sign/publish steps.
on:
  workflow_dispatch:

jobs:
  not-configured:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Signed releases are not configured yet."
          echo "See CHANGELOG.md (Deferred) and DEPLOYMENT.md; wire publish creds + signing keys to enable."
